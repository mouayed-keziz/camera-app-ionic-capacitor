import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'camera-app-ionic-capacitor',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
